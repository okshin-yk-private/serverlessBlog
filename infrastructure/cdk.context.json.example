{
  "comment": "IMPORTANT: Copy this file to cdk.context.json and update with your actual credentials",
  "warning": "Never commit cdk.context.json to version control! It's in .gitignore for security.",

  "basicAuth": {
    "username": "your-username-here",
    "password": "your-secure-password-here"
  },

  "usage": {
    "dev": "Basic Auth is enabled ONLY in DEV environment",
    "dev": "Basic Auth is disabled in dev environment",
    "prd": "Basic Auth is disabled in production environment",
    "deployment": "Deploy with: cdk deploy --context stage=dev",
    "credentials": "Credentials are embedded in CloudFront Function code at deployment time"
  }
}

# Agent instruction sources

This legacy file is no longer a generation source. Shared rules live in
`docs/ai-shared-rules.md`; CLI-specific entrypoints are `AGENTS.md` and `CLAUDE.md`.
`scripts/sync-ai-docs.ts` validates those references and does not overwrite them.
